apiVersion: apps/v1
kind: Deployment
metadata:
  name: get-chats-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: get-chats-service

  template:
    metadata:
      labels:
        app: get-chats-service

    spec:
      containers:
        - name: get-chats-service
          image: mikroservices-get-chats-service
          imagePullPolicy: IfNotPresent

          ports:
            - containerPort: 8000

          env:
            - name: DATABASE_URL
              value: postgresql+psycopg2://postgres:yusufboy2012@my-postgres:5432/my-db

            - name: REDIS_URL
              value: redis://redis:6379
---
apiVersion: v1
kind: Service
metadata:
  name: get-chats-service

spec:
  selector:
    app: get-chats-service

  ports:
    - port: 8000
      targetPort: 8000

  type: ClusterIP