apiVersion: apps/v1
kind: Deployment
metadata:
  name: create-product-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: create-product-service

  template:
    metadata:
      labels:
        app: create-product-service

    spec:
      containers:
        - name: create-product-service
          image: mikroservices-create-product-service
          imagePullPolicy: IfNotPresent

          ports:
            - containerPort: 8000

          env:
            - name: DATABASE_URL
              value: postgresql+psycopg2://postgres:yusufboy2012@my-postgres:5432/my-db

            - name: REDIS_URL
              value: redis://redis:6379
---
apiVersion: v1
kind: Service
metadata:
  name: create-product-service

spec:
  selector:
    app: create-product-service

  ports:
    - port: 8000
      targetPort: 8000

  type: ClusterIP