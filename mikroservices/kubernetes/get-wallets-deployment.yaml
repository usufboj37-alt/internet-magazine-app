apiVersion: apps/v1
kind: Deployment
metadata:
  name: get-wallets-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: get-wallets-service

  template:
    metadata:
      labels:
        app: get-wallets-service

    spec:
      containers:
        - name: get-wallets-service
          image: mikroservices-get-wallets-service
          imagePullPolicy: IfNotPresent

          ports:
            - containerPort: 8000

          env:
            - name: DATABASE_URL
              value: postgresql+psycopg2://postgres:yusufboy2012@my-postgres:5432/my-db

            - name: REDIS_URL
              value: redis://redis:6379
---
apiVersion: v1
kind: Service
metadata:
  name: get-wallets-service

spec:
  selector:
    app: get-wallets-service

  ports:
    - port: 8000
      targetPort: 8000

  type: ClusterIP