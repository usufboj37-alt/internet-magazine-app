apiVersion: apps/v1
kind: Deployment
metadata:
  name: add-cart-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: add-cart-service

  template:
    metadata:
      labels:
        app: add-cart-service

    spec:
      containers:
        - name: add-cart-service
          image: mikroservices-add-cart-service
          imagePullPolicy: IfNotPresent

          ports:
            - containerPort: 8000

          env:
            - name: DATABASE_URL
              value: postgresql+psycopg2://postgres:yusufboy2012@my-postgres:5432/my-db

            - name: REDIS_URL
              value: redis://redis:6379
---
apiVersion: v1
kind: Service
metadata:
  name: add-cart-service

spec:
  selector:
    app: add-cart-service

  ports:
    - port: 8000
      targetPort: 8000

  type: ClusterIP