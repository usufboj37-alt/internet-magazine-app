apiVersion: apps/v1
kind: Deployment
metadata:
  name: get-product-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: get-product-service

  template:
    metadata:
      labels:
        app: get-product-service

    spec:
      containers:
        - name: get-product-service
          image: mikroservices-get-product-service
          imagePullPolicy: IfNotPresent

          ports:
            - containerPort: 8000

          env:
            - name: DATABASE_URL
              value: postgresql+psycopg2://postgres:yusufboy2012@my-postgres:5432/mydb

            - name: REDIS_URL
              value: redis://redis:6379
---
apiVersion: v1
kind: Service
metadata:
  name: get-product-service

spec:
  selector:
    app: get-product-service

  ports:
    - port: 8000
      targetPort: 8000

  type: ClusterIP